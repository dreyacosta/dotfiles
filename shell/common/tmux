# Load tmux
if command -v tmux &>/dev/null \
  && [[ -z "${TMUX:-}" ]] \
  && [[ -z "${HERDR_PANE_ID:-}" ]] \
  && [[ -n "${PS1:-}" ]] \
  && [[ $- == *i* ]] \
  && [[ $(tty) != "not a tty" ]]; then
  tmux a || tmux new-session -s dreyacosta
fi
